files:
    "/etc/cron.d/clear_cache":
        mode: "000644"
        owner: root
        group: root
        content: |
            0 0 * * 0 root /usr/bin/php /var/app/current/artisan cache:clear

commands:
    remove_old_cron:
        command: "rm -f /etc/cron.d/clear_cache.bak"
